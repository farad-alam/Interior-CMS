// Numbered section variants (Hero01, Portfolio01, Testimonials01, ...) live
// here. Every variant is written against the shared contracts in
// @amarsoftware/cms-client so it accepts the same data shape (plan Phase 7).
// A variant only "graduates" into this package when a second client needs
// it — until then it stays local to a client's app. Empty for now.
export {}
