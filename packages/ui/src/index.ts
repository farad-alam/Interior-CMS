// Design-system primitives (Button, Card, Container, Grid, ...) live here.
// Per the plan's "don't over-build" rule (Phase 0.5 / Additional Notes),
// this stays empty until real primitives are extracted from the pilot
// client's build. Placeholder export keeps the package valid meanwhile.
export {}
